# {{ ansible_managed }}

CHECK PROCESS {{ services['supervisor']['name'] }} MATCHING "{{ services['supervisor']['regexp'] }}"
  IF NOT EXIST FOR 1 CYCLES THEN {{ services['supervisor']['action'] }}
{% if services['supervisor']['action'] != "alert" %}
  REPEAT EVERY {{ services['supervisor']['repeat_interval'] }} CYCLES
{% endif %}
  group supervisor

